<%= simple_form_for([@booking, @review]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :content %>
    <%= f.input :rating %>
  </div>

  <%= f.button :submit,  'Add review', class: 'btn btn-treehouse'%>
  </div>
<% end %>
